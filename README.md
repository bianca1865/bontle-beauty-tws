# Bontle Beauty TWS

## Setup
1. Run `npm install`
2. Run `npm run dev`

## Database
- Schema in `schema.sql`
- Tables: stylists, appointments

## Endpoints
- POST /auth/register
- POST /auth/login
- POST /stylists
- GET /stylists
- POST /appointments
- GET /appointments

